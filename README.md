# Explainable-Sentiment-Analysis-with-DistilBERT-and-SHAP
This project demonstrates how Explainable AI (XAI) techniques can be used to interpret the predictions of a transformer-based text classification model. A DistilBERT model is fine-tuned on the IMDb movie review dataset to perform binary sentiment classification (positive or negative). 
